import PyQt5


def availSearch(self):
    availDate
    aDate = int(self.availDate.toComboBox())
    availPeriod
    availRoom
    print(aDate)


self.availButton.clicked.connect(self.availSearch)


def specSearch(self):
    specDate
    specPeriod
    specRoom


self.specButton.clicked.connect(self.specSearch)
